Hoisting - Hoisting means javascript moves variables and functions declaration to the top of their scope before running the code.

Closures - A closure is when a function remembers the variables from its outer function(the scope it was created in), even if that outer function (scope) has finished running.


Callback fn - A callback function is a function that is passed as an argument to another function, so that it can be called later or after some events.